app.controller('StaticPagesCtrl', ['$scope', '$auth', 'UserService', function($scope, $auth, UserService){
  $scope.message = "Hello World!";
  // $scope.logout = function () {
  //   $auth.signOut().then(function(resp) {
  //     console.log(resp);
  //   }).catch(function(resp) {
  //     console.log(resp);
  //   });
  // };
  $scope.user = UserService.user;
  console.log($scope.user)
}]);