app.controller('StaticPagesCtrl', ['$scope', '$auth', 'UserService', '$location', function($scope, $auth, UserService, $location){

  $scope.message = "Hello World!";
  $scope.adminCheckFirst = function () {
    if ($auth.user.admin){
      $location.path('order_dashboard')
    };
  };
  $scope.adminCheckFirst();
}]);